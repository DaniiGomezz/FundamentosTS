// 05-funcion-con-tipado.ts

// Definir la función 'add' con parámetros y tipo de retorno
function add(a: number, b: number): number {
    return a + b;
}

// Ejemplo de uso
const result = add(5, 10);
console.log(`La suma de 5 y 10 es: ${result}`);

